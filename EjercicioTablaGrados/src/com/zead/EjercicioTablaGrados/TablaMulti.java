package com.zead.EjercicioTablaGrados;

public interface TablaMulti {
    // no se pueden crear objetos, ni constructores, unicamente las cabeceras de los metodos
    public void tabla();
    public int estudiantes(int estudiante);

}
